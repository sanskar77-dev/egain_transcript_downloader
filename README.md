# egain_transcript_downloader
Direct-API downloader for eGain AI transcript exports (Specsavers "Domiciliary" bot-sessions), plus a combiner merging paged CSV output into one Excel file. Replaces the manual UI export loop with concurrent authenticated API calls and automatic retry/recovery for empty pages.
